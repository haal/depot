$("#notice").hide();
$('#cart').html("<%= escape_javascript(render(@cart)) %>");
<% if @cart.total_items == 1 %>
  $('#cart').show('slide', { direction: "down" } , 500);
<% end %>
$('#current_item').effect("highlight", { color: "#88ff88" }, 3000);